<div class="row mb-4">
    <div class="col-6 p-0">
        <h2>{{title}}</h2>
    </div>
    <div class="col-6 p-0 align-self-center text-end">
        <a href="/question/create" class="btn btn-sm btn-outline-success my-1">
            <span class="icon-plus"></span> Adicionar
        </a>
    </div>
</div>

{{> messages}}

{{#if questions}}
    <div class="row fw-bold bg-primary-subtle text-black py-2">
        <div class="col-1 text-center">ID</div>
        <div class="col-6 col-lg">Questão</div>
        <div class="col-5 col-lg-2 text-center">Ações</div>
    </div>
    <span class="zebra">
        {{#each questions}}
            <div class="row py-2 border-bottom align-items-center">
            <div class="col-1 text-center">
                {{id}}
            </div>
            <div class="col-6 col-lg">
                {{name}} <small>({{Activity.name}})</small>
            </div>
            <div class="col-5 col-lg-2 text-center">
                <a href="/answer?question={{id}}" class="mx-2 text-decoration-none" title="Respostas de {{name}}">
                    <span class="icon-list text-black"></span>
                </a>
                <a href="/question/edit/{{id}}" class="mx-2 text-decoration-none" title="Editar">
                    <span class="icon-pencil text-primary"></span>
                </a>
                <a href="#" class="mx-1 text-decoration-none" data-bs-toggle="modal" data-bs-target="#destroyModal"
                   data-bs-text="{{name}}" data-bs-id="/question/delete/{{id}}" title="Excluir">
                    <span class="icon-trash text-danger"></span>
                </a>
            </div>
        </div>
        {{/each}}
    </span>
{{else}}
    {{> alert-new-register link="/question/create" name="questão"}}
{{/if}}

{{> modal-delete link="/question/delete/{{id}}" name="questão"}}
